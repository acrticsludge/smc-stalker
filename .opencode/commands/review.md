---
name: review
description: Surgical code review of staged or recent changes.
---

# Review Command

Run: `git diff --staged` or `git diff HEAD~1`

Use the `reviewer` agent on the output. Flag CRITICAL issues first. Do not rewrite — only flag.
