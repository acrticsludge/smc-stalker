# ============================================================
# .env.example — Merged from root + frontend .env.local
# Copy to .env.local and fill in your values.
# For Vercel deployment: set these in Vercel Dashboard →
#   Settings → Environment Variables
# ============================================================

# ── Supabase ────────────────────────────────────────────────
# Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
SUPABASE_JWT_SECRET=your_supabase_jwt_secret

# MCP: Dashboard → Account → Access Tokens
SUPABASE_ACCESS_TOKEN=your_supabase_access_token
# MCP: Dashboard → Settings → General
SUPABASE_PROJECT_REF=your_project_ref

# ── Database (direct connection) ────────────────────────────
# Dashboard → Settings → Database → Connection string (URI mode)
DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# ── Vercel ──────────────────────────────────────────────────
# vercel.com/account/tokens
VERCEL_TOKEN=your_vercel_token
# vercel.com → Project Settings → General
VERCEL_PROJECT_ID=your_vercel_project_id
VERCEL_ORG_ID=your_vercel_org_id

# ── Railway ─────────────────────────────────────────────────
# railway.app/account/tokens
RAILWAY_API_TOKEN=your_railway_api_token

# ── Resend ──────────────────────────────────────────────────
# resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key
# The from-address you've verified in Resend
RESEND_FROM_EMAIL=alerts@your-domain.com

# ── GitHub ──────────────────────────────────────────────────
# github.com/settings/tokens — scopes: repo, read:org
GITHUB_TOKEN=ghp_your_github_token

# ── Dodo Payments ───────────────────────────────────────────
# app.dodopayments.com → Developers → API Keys
DODO_API_KEY=your_dodo_api_key
DODO_WEBHOOK_SECRET=your_dodo_webhook_secret

# ── DeepSeek ────────────────────────────────────────────────
# platform.deepseek.com → API Keys
DEEPSEEK_API_KEY=sk_your_deepseek_api_key

# ── Hugging Face ────────────────────────────────────────────
# huggingface.co/settings/tokens
HF_TOKEN=hf_your_huggingface_token

# ── ElevenLabs ──────────────────────────────────────────────
# elevenlabs.io → Profile → API Keys
ELEVENLABS_API_KEY=sk_your_elevenlabs_api_key

# ── fal.ai ──────────────────────────────────────────────────
# fal.ai/dashboard/keys
FAL_KEY=your_fal_key

# ── Deepgram ────────────────────────────────────────────────
# console.deepgram.com → API Keys
DEEPGRAM_API_KEY=your_deepgram_api_key

# ── Gemini ──────────────────────────────────────────────────
# aistudio.google.com → API Keys
GEMINI_API_KEY=AIza_your_gemini_api_key

# ── Replicate ───────────────────────────────────────────────
# replicate.com/account/api-tokens
REPLICATE_API_KEY=r8_your_replicate_api_key

# ── TTS Backend URLs ──────────────────────────────────────
# Chatterbox TTS (local Colab or self-hosted)
CHATTERBOX_BASE_URL=https://your-chatterbox.ngrok-free.dev

# FishSpeech S2 Pro (GCloud VM or self-hosted)
FISHSPEECH_BASE_URL=http://your-vm:8080

# ── App ─────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://voxslide.pulsemonitor.dev
NODE_ENV=development

# ── SMC Stalker Bot (Discord) ──────────────────────────────
# Discord Developer Portal → Application → Bot → Token
# https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token
# Same page → General Information → Application ID
DISCORD_CLIENT_ID=your_discord_client_id

# ── SMC Stalker Bot (Dynmap) ───────────────────────────────
# Towny dynmap markers endpoint
DYNMAP_URL=https://map.sovereignmc.net/maps/world/live/markers.json
# Request timeout in milliseconds (default: 30000)
DYNMAP_TIMEOUT_MS=30000

# ── SMC Stalker Bot (Logging) ──────────────────────────────
# Log level: trace | debug | info | warn | error | fatal
LOG_LEVEL=info

# ── Encryption ──────────────────────────────────────────────
# Random 32-byte hex string (use `openssl rand -hex 32` to generate)
ENCRYPTION_KEY=your_32_byte_hex_encryption_key

# ── Push Notifications (Web Push API) ───────────────────────
# Generate with: npm install -g web-push && web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
VAPID_SUBJECT=mailto:your-email@example.com
